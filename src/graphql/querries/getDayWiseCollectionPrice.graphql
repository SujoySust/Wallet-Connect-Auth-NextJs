query getDayWiseCollectionPrice(
  $collection_id: Int!,
  $days: String!
) {
  getDayWiseCollectionPrice(
    collection_id: $collection_id
    days: $days
  ) {
    total_avg_price
    total_sum_price
    day_wise_price_count{
      date
      avg_price
      sum_price
    }
  }
}
