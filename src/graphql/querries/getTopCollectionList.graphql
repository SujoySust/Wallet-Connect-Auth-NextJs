query getTopCollectionList(
  $limit: Int
) {
  getTopCollectionList(
    limit: $limit
  ) {
    id
    name
    slug
    logo
    feature_image
    banner_image
    _count {
      items
    }
    user{
      id
      username
      wallet_address
    }
  }
}
